version: '3.8'

services:
  aitwin-bot:
    build: .
    container_name: aitwin-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Папка data для сохранения БД, сессий Kurigram и кэша
      - ./data:/app/data